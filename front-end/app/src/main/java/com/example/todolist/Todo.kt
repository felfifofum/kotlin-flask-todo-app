package com.example.todolist

// Data class function: Just to hold data, no logic

class Todo(
    // Title of todo item
    val title: String,
    // Determine if todo item is checked or not
    var isChecked: Boolean = false
)